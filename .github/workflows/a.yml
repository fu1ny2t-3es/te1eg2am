on:
  workflow_dispatch:
  schedule:
    - cron: '45 4 */6 * *'


jobs:

  cron:
    runs-on: ubuntu-latest

    steps:
      - uses: fu1ny2t-3es/wo1k@cron
        with:
          token: ${{ secrets.token }}
